# Environment / secrets
.env
*.env.local
# Note: .secrets.baseline IS committed (detect-secrets tracks reviewed false positives, not real secrets).

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
db.sqlite3
staticfiles/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# Node / frontend
node_modules/
dist/
frontend/.vite/

# Editors / OS
.vscode/
.idea/
.DS_Store

# Terraform
**/.terraform/
*.tfstate
*.tfstate.backup
.terraform.lock.hcl
citramac-hmis-copilot-docs/  # original delivered spec package — superseded by /docs, /mockups, /.github at repo root; kept on disk, not tracked
